[package]
name = "unit_ranged"
version = "0.4.0"
edition = "2024"

[features]
default = []
std = []
serde = ["dep:serde"]

[dependencies]
num-traits = "0"
serde = { version = "1.0", optional = true, default-features = false }

[dev-dependencies]
serde_json = "1.0"
serde_derive = "1.0"
